# Default values for node-rest-api.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ronaldcrb/node-pickard
  tag: latest
  pullPolicy: IfNotPresent


service:
  type: ClusterIP
  port: 3000


resources: {}
  # limits:
  #   cpu: 100m
  #   memory: 128Mi
  # requests:
  #   cpu: 100m
  #   memory: 128Mi

envVars: {}

secrets: {}
